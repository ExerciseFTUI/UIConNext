import { View, Text } from "react-native";
import React from "react";

const Card = () => {
  return (
    <View className="shadow-xl p-3 bg-slate-50 rounded-lg">
      <Text className="text-xl">Card</Text>
    </View>
  );
};

export default Card;
